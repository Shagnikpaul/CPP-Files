#include "stdio.h"
#include "stdlib.h"

typedef struct Node
{
    int* arr;
    struct Node *prev;
    struct Node *next;
} Node;



typedef struct LinkedList {
    struct Node* head;
} LinkedList;



void addAtEnd(LinkedList l*){
    scanf("n")
    Node *n = malloc(sizeofNode)

    n->arr = malloc(n * sizeof(int));

    for(0 to n)
    scanf(ele1)
    n->arr[i] = ele1;


    



    
}


int main()
{
  //// write da code here... 
  int n = 9;
  int m = 9;
  int mat[9][9]; 
  for(int i=0;i<m;i++){
    for(int j=0;j<n;j++){
        if(mat[i][j] != 0){
            addAtBeginning(LinkedList l*, i, j, mat[i][j]);
        }
    }
  } 
  return 0;
}