#include <iostream>
#include<stack>

#include "list"
using namespace std;
int main()
{
    stack<string> str;
    str.push("SHA");
    str.push("SHA2");
    str.push("SHA3");
    str.push("SHA4");

    // remove top element s.pop()

    // size of stack s.size()

    // is the stack empty ? s.empty() returns 0 or 1

    
}